// app.config.ts
import { defineConfig } from "@tanstack/start/config";
var app_config_default = defineConfig({
  vite: {
    // Basic config to test loading
  }
});
export {
  app_config_default as default
};
